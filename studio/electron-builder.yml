appId: com.patchr.studio
productName: Patchr Studio
directories:
  buildResources: build
publish:
  provider: github
  owner: DeepFoldProtein
  repo: patchr
icon: icon.png
files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{.env,.env.*,.npmrc,pnpm-lock.yaml}"
  - "!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}"
asarUnpack:
  - resources/**
win:
  executableName: patchr-studio
  icon: icon.png
  target:
    - target: nsis
      arch:
        - x64
nsis:
  # Version-less name so /releases/latest/download/patchr-studio-setup.exe is stable.
  artifactName: ${name}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  # productName has a space, so the default zip name is "Patchr Studio-...zip".
  # electron-builder writes that into latest-mac.yml with spaces as hyphens,
  # while GitHub renames the uploaded asset with dots — the two never matched
  # and every macOS auto-update 404'd. Pin a space-free name so the feed and
  # the asset agree. dmg keeps its own artifactName below.
  artifactName: ${name}-${version}-${arch}-mac.${ext}
  icon: icon.png
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: true
dmg:
  # Version-less so /releases/latest/download/patchr-studio.dmg is a stable link.
  artifactName: ${name}.${ext}
linux:
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  maintainer: electronjs.org
  category: Utility
appImage:
  # Version-less so /releases/latest/download/patchr-studio.AppImage is stable.
  artifactName: ${name}.${ext}
npmRebuild: false
