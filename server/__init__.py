"""Support package for the patchr inference server.

The runnable entrypoint stays at repo-root ``server.py`` (run as ``python
server.py``); this package holds pieces split out of that file to keep it
manageable — Pydantic models, and the daily-activity dashboard.
"""
